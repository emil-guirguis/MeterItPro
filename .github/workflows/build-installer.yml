name: Build Sync Installer

on:
  push:
    branches: [main]
    paths:
      - 'MeterItProSync/installer/**'
      - 'MeterItProSync/docker-compose.yml'
      - 'MeterItProSync/docker/init/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache-dependency-path: MeterItProSync/installer/go.mod

      - name: Copy embedded files into installer
        run: |
          cp MeterItProSync/docker-compose.yml  MeterItProSync/installer/docker-compose.yml
          cp MeterItProSync/docker/init/00-schema.sql MeterItProSync/installer/00-schema.sql

      - name: Build Windows amd64 installer
        run: |
          cd MeterItProSync/installer
          GOOS=windows GOARCH=amd64 go build -o MeterItPro-SyncSetup.exe .

      - name: Build Linux amd64 installer
        run: |
          cd MeterItProSync/installer
          GOOS=linux GOARCH=amd64 go build -o MeterItPro-SyncSetup-linux .

      - name: Package autoinstall USB files
        run: |
          cd MeterItProSync/installer
          zip -r MeterItPro-autoinstall.zip autoinstall/

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: MeterItPro-SyncSetup-Windows
          path: MeterItProSync/installer/MeterItPro-SyncSetup.exe
          retention-days: 90

      - name: Upload Linux artifact
        uses: actions/upload-artifact@v4
        with:
          name: MeterItPro-SyncSetup-Linux
          path: MeterItProSync/installer/MeterItPro-SyncSetup-linux
          retention-days: 90

      - name: Upload autoinstall USB files artifact
        uses: actions/upload-artifact@v4
        with:
          name: MeterItPro-autoinstall
          path: MeterItProSync/installer/MeterItPro-autoinstall.zip
          retention-days: 90
