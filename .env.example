# ============================================================================
# .env.example — Full template documenting every variable
# Copy to .env and fill in real values. Do NOT commit .env.
# ============================================================================

NODE_ENV=development
LOG_LEVEL=debug

# ============================================================================
# VITE DEV AUTO-LOGIN (sync/frontend development only)
# ============================================================================
VITE_DEV_AUTO_LOGIN=true
VITE_DEV_EMAIL=admin@example.com
VITE_DEV_PASSWORD=changeme

# ============================================================================
# DATABASE — Client (remote, e.g. Supabase)
# ============================================================================
POSTGRES_CLIENT_HOST=your-supabase-host.pooler.supabase.com
POSTGRES_CLIENT_PORT=6543
POSTGRES_CLIENT_DB=postgres
POSTGRES_CLIENT_USER=postgres.your-project-ref
POSTGRES_CLIENT_PASSWORD=your-client-db-password

# ============================================================================
# DATABASE — Sync (local PostgreSQL)
# ============================================================================
POSTGRES_SYNC_HOST=localhost
POSTGRES_SYNC_PORT=5432
POSTGRES_SYNC_DB=postgres
POSTGRES_SYNC_USER=postgres
POSTGRES_SYNC_PASSWORD=your-sync-db-password

# ============================================================================
# API URLs
# ============================================================================
# Backend Client API URL (used by Sync MCP server)
# Dev: http://localhost:8787/api   Prod: https://client.meterit.com/api
CLIENT_API_URL=http://localhost:8787/api
CLIENT_API_KEY=

# Vite frontend Client API URL
VITE_CLIENT_API_URL=http://localhost:8787/api

# ============================================================================
# JWT CONFIGURATION
# ============================================================================
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# ============================================================================
# SYNC API SERVER
# ============================================================================
SYNC_API_PORT=3002

# ============================================================================
# SYNC MCP CONFIGURATION
# ============================================================================
SYNC_INTERVAL_MINUTES=5
BATCH_SIZE=1000
MAX_RETRIES=5

# ============================================================================
# BACNET CONFIGURATION
# ============================================================================
BACNET_INTERFACE=0.0.0.0
BACNET_PORT=47808

# ============================================================================
# METER READING CLEANUP
# ============================================================================
METER_READING_RETENTION_DAYS=60
METER_READING_CLEANUP_AUTO_START=true

# ============================================================================
# MCP HTTP SERVER (internal HTTP server for BACnet agent communication)
# ============================================================================
MCP_HTTP_PORT=3003
MCP_HTTP_URL=http://127.0.0.1:3003

