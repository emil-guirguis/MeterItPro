# ============================================================================
# PRODUCTION OVERRIDES — only values that differ from .env dev defaults
# Loaded after .env with override: true
# ============================================================================

NODE_ENV=production
LOG_LEVEL=info
CLIENT_API_URL=https://meteritpro.com/api
VITE_DEV_AUTO_LOGIN=false
